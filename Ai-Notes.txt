# Project Status Notes

This file is the AI launch point for a new session: keep it ALWAYS up to date.
Update the Date and any changed sections whenever design, firmware, docs, or
CI/release workflows change - don't let it go stale.

## THE DEVICE: PIC18F47Q10, AND NOTHING ELSE (user instruction, 2026-09-23)
**PIC18F47Q10 ONLY. There is no second device, no "legacy part", and no device to choose
between.** The PIC16F18875 port was removed on 2026-09-23: its `#if defined(__18F47Q10__)` code
paths, its `-DPICAMP_DEVICE=PIC16F18875` build option, its CI matrix entry, its `.vscode` defines
and include paths, its `run_sim.sh`/`run_sim.ps1` default device, and its entries in these notes
were all deleted. Concretely, that means:
- Do not re-add a device guard, a per-device lookup table, or a second `PICAMP_DEVICE_*` fact block
  "just in case". `cmake/My_Pic_Project/default/device.cmake` states one device, and the harnesses
  carry one instruction-rate constant (Q10: 1625 instructions per simulated millisecond).
- Historical material about the 16F port, and the abandoned PIC16F18877 evaluation, now lives only
  in `prototype_reference/` and `bugfixes.md`. Do not restate it in `docs/`, `README.md` or here.
- If a document, comment or test disagrees with `device.cmake` about the target part, `device.cmake`
  wins and that document is the bug.
- The one deliberate exception: `tools/setup/parameterise_device.py` still *pattern-matches* the 16F
  tokens, because MPLAB X emits them when it regenerates `.generated/rule.cmake` from the MPLAB
  project the tree was created from. Those are the generator's tokens, not our target - removing the
  match would let a regenerated rule.cmake silently build the wrong part.

## Standing AI instructions (things the user asked to always remember)
- EVERY SIMULATOR TEST RUNS THROUGH THE WATCHDOG - ALWAYS (user instruction, 2026-09-23: "ALL these
  tests should run through the watchdog -- always"). The watchdog is
  `tools/simulate/run_suite_with_watchdog.py`; `ctest --test-dir _build/My_Pic_Project/release
  --output-on-failure` is the canonical local entry point (both registered tests route through the
  watchdog), and `run_suite_with_watchdog.py --test suite|first-dit` is the direct one. Never run
  `test_first_dit.py` / `trace_ptt_sequence.py` bare and never drive a test with raw `mdb.sh`: a bare
  run has no timeout, no heartbeat (the log tab the user watches never moves), no orphan clean-up,
  and its verdict lands outside the run's log. A new test must be registered in
  `cmake/My_Pic_Project/default/user.cmake` AND given a `--test` choice, or the watchdog cannot wrap
  it.
- TEST RUNS ARE NEVER OBSERVED THROUGH THE TERMINAL, AND NEVER ASK A RUN FOR ITS TERMINAL OUTPUT -
  VERDICTS COME FROM FILES (user instruction, restated 2026-09-21 and again 2026-09-23: "we should
  not ask for terminal output when running tests -- this breaks the terminal. We are using a
  file-based approach instead."). Asking a running test for its output wedges the terminal and
  returns nothing usable, so this is a hard rule, not a preference; the whole verdict method is
  file-based: redirect the run into a log, append the exit code, read the log + the exit-code line +
  the watchdog progress log. Do
  not run a test to watch its output, do not `tail`/`grep` a test log into the terminal, and do not
  ask a run for its terminal output. There is far too much data (MDB emits megabytes of pin/state
  trace) and the capture is unreliable. The terminal is only for *launching* a run and for short,
  bounded process checks (`pgrep`, `ps`, `wc -c`). Every verdict, progress report and diagnostic is
  read from the run's files: the log it was redirected into, the exit-code line appended to it, and
  the watchdog progress logs. Two traps: a terminal command that returns no output says nothing
  about the run, and `read_file` can serve a stale cached copy of a log that is still being written,
  which also looks like "the run produced nothing" - copy the log to a fresh path and read that.
  The mechanics (exact commands and the redirect/exit-code pattern) live in
  `.github/skills/build-test/SKILL.md` and TESTING.md, deliberately not restated here.
- Keep this file (Ai-Notes.txt) ALWAYS up to date; it's the launch point for a new session.
- ALWAYS READ `deepseek-pic.md` IN THE REPOSITORY ROOT AT THE START OF EVERY SESSION (user
  instruction, 2026-09-22, permanent). It is a bare-metal PIC guardrail checklist - TRIS vs LAT,
  brief ISRs, `volatile` on ISR-shared globals, never guess config words, and a test-harness
  protocol - written by another agent, and the user wants it used as a cross-check on this
  project's work. Treat it as a *checklist*, not as project state: this file and
  `.github/skills/build-test/SKILL.md` remain the sources of truth, and its own
  `<session_state>` block ("Awaiting the first hardware assignment") is stale and does not describe
  this project.
- FOR LONG-RUNNING JOBS, WRITE THE LOG TO A FILE AND FOLLOW IT IN A VS CODE *TAB* WITH FILETAIL -
  NEVER TAIL IT IN THE CONSOLE (user instruction, 2026-09-22, sticky/permanent, restated several
  times as the method was refined: generalised to all long jobs, then "tail mode", then "delete and
  recreate first", then "not in the console, open it in a tab", then "use FileTail"). The final
  method: truncate the old log (`Clear-Content` - the launcher appends now, and deleting a file a
  watcher has open kills the watcher), write the job's output there, open that file in a VS Code tab,
  and toggle **FileTail** (`filetail.toggle`) ONLY while a job is actually running. **FileTail is
  conditional now, not mandatory - this supersedes the earlier "must be installed" rule (user
  instruction, 2026-09-22). It measured ~110% of one core in the VS Code extension host plus ~24% in
  the main process while following a log; toggling it off dropped both to roughly 1%. A tool that eats
  a core to follow a text file is badly written and must not be used: turn it off when the run ends,
  and if it shows that load again stop using it and read the log on demand instead (a one-shot
  `Get-Content -Tail 20` is a read, not a console tail).** A console tail
  is forbidden, and it was also silently broken: `Get-Content -Wait` holds the handle to the file it
  opened, and the suite launcher unlinks and recreates its log at startup, so a console tail started
  before the run reads a deleted file and shows nothing. This applies to every long job - suites,
  cmake builds, ctest runs, spike runs, anything taking minutes. For the mdb suite the file that
  "updates often with progress" is the watchdog heartbeat log (`%TEMP%\picampcontrol_suite_progress.log`
  on Windows, `/tmp/...` on macOS), NOT the ctest log: ctest prints nothing until a test completes, so
  its log sits at four lines for the whole run and looks hung. Watching a log for visibility is
  sanctioned; judging a run from terminal output is not - verdicts are still read from the run's own
  log file and exit-code line.
- KEEP TOKEN AND CPU USE DOWN - OUTPUT DISCIPLINE (user instruction, 2026-09-22, sticky/permanent).
  The chat transcript is re-rendered on every streamed token: this session reached 2,041 lines / 2.2 MB,
  and that drove the renderer to ~225% of one core and the extension host to ~112% for the whole of
  each assistant turn. So: answer in as few words as the answer allows and never restate the context;
  never paste file contents, logs, tables of raw output, or exit-code dumps into the chat unless asked;
  put findings in the run's log file, the skill, or a repo doc and report one line plus the path; batch
  work into fewer, longer tool calls rather than many small ones; filter command output (`-Tail`,
  `-First`, `Select-String`) instead of dumping whole files into the terminal panel, which is rendered
  too; do not print a code block unless the user asked for it. The skill is where durable knowledge
  goes - not the chat.
- FOLLOWING A GROWING FILE IS OURS NOW (user instruction, 2026-09-23: "we need a decent tail plugin" -
  the CPU-eater that was rejected is `spacetown.filetail`, ~110% of one core in the extension host).
  The capability is the in-repo extension `tools/logfollower` (Log Follower): one `stat()` per poll
  (400 ms default - package.json, the code fallback and the README now all agree; they previously
  said 250/400/60), a revert plus one scroll only on real growth, and nothing at all while paused or
  while the followed tab is not the visible one. The user watches a run by having its log open in a
  VS Code tab: `tools/simulate/open_progress_log.py <log>` opens it, and
  `tools/simulate/run_detached.py` starts a run in its OWN session so the agent's terminal harness
  cannot SIGTERM it mid-run - it did exactly that twice on 2026-09-23, and the damage reads as
  `SUITE_EXIT=143` with the log stopping mid-file, i.e. like a test failure when it is not one.
  Pause is PER RUN: interaction with the text or the scrollbars (click, wheel, scrollbar drag,
  highlighting text) stops the follow for that run only, and the next run's truncation re-arms it
  automatically. Still to do when there is slack: fold the generic method into its own skill, since
  it is not PicAmpControl-specific.
- DO NOT ASK FOR PERMISSION TO OVERWRITE FILES - JUST DO IT (user instruction, 2026-09-22). The user
  does not want to be prompted about overwriting, nor asked "say the word" before an edit that was
  already agreed; when the intent is to change a file, change it. Related and worth avoiding: writing
  to a file the user has open in an editor tab makes VS Code raise its own "file changed on disk"
  prompt, so clear/recreate such a log *before* opening it in the editor rather than after.
- FLASH REDUCTION IS A STANDING WORK ITEM (user instruction, 2026-09-21). A near-full Debug image
  is not an acceptable steady state: when a build or test leaves you waiting, spend the time finding
  space. Inline assembly is allowed if every block carries an equivalent-C comment for review. The
  point of the headroom is to be able to assert strongly - and with test evidence - that the PTT
  sequencing cannot damage the amplifier, so no gaps and no excuses in the test coverage. Levers and
  the policy detail live in the skill's `## Flash space`.
- COMMIT AND PUSH OFTEN - LOCAL AND REMOTE (user instruction, restated 2026-09-21 because it was
  forgotten). Commit and push to `origin/main` as soon as each change is verified green; do not
  batch a session's work into one large commit at the end, and never leave verified work sitting
  only in the working tree. The only acceptable reason to hold a commit is a verdict that is still
  running, and then it is committed the moment that verdict reads green. Always push to origin/main
  after a successful compile/build of a change. Firmware only truly compiles via CI
  (firmware-build.yml, which runs on ubuntu-latest), so this means: commit, push, and confirm the
  triggered build succeeds.
- DELETE LARGE FILES WHEN DONE WITH THEM, INCLUDING IN /tmp (user instruction, 2026-09-21). Test
  logs and MDB transcripts are megabytes; remove them (`rm -f /tmp/<log>`) as soon as the verdict
  has been read. Keep a log only while its run's verdict is still in use, and never accumulate a
  series of run logs (the stale-read workaround of copying a log to a second path doubles the
  space, so delete both).
- Keep README.md and docs/ purely current-facing: no "old prototype was replaced by..." style historical narrative. Historical context belongs only in prototype_reference/.
- Never run bare `git status`/`git status --short`; use `git --no-pager status --short` (avoids hitting an interactive pager or the `gcm-core` credential helper prompt).
- NEVER WRITE "Hmmm" - INCLUDING IN YOUR INTERNAL REASONING (user instruction, 2026-09-22, restated the same day because it was still happening, and repeated AGAIN twice more the same day after this rule existed - once in a reasoning trace the user quoted straight back). The user can see the thinking as well as the reply, so this covers reasoning traces, preambles to tool calls, and final answers alike. Do not use it as filler, as a thinking-out-loud opener, or as a hedge. State the finding, or the uncertainty, plainly. It was missed once already by reading "anywhere in a reply" as excluding the thinking - the first fix was too narrow, so do not repeat that misreading. The rule is now ALSO stated at the top of `.github/skills/build-test/SKILL.md` as a hard writing rule, because a bullet in a long list demonstrably was not enough.
- docs/hardware/PIC18F47Q10_pin_map_and_setup.md is the SINGLE source of truth for MCU pin assignments. Never restate pin numbers or pin tables in README, architecture, schematic-package, or checklist docs - link to it instead. Keep it in sync with firmware/include/pin_map.h. The same rule applies to nets (project_schematic_package/connection_table.csv) and the BOM (component_list.csv).
- Any new "remember this" instruction from the user must be added to this section, not just kept in assistant memory.
- ALWAYS WRITE SILLY MISTAKES INTO THE SKILL, EVERY TIME - NOT JUST IN THE SESSION THEY HAPPENED
  (user instruction, 2026-09-22). Whenever a mistake costs a cycle, feed the lesson back into
  `.github/skills/build-test/SKILL.md` as part of that same piece of work. The user's
  words: "Always write to the skill for silly mistakes like this. Always. Not just in this session."
  Silly mistakes are precisely the ones that recur, and a lesson left only in this file or only in
  the conversation that produced it does not reach the next session.
- [HISTORY - superseded 2026-09-23; PIC18F47Q10 is the only device now] DEVICE UPGRADE PIC16F18877
  EVALUATED, *PROVISIONALLY* REJECTED - THE VERDICT IS NOT SOUND
  (2026-09-22; requalified later the same day, see the Q10 entry below). PIC16F18877-I/P (same
  family, 40-pin PDIP, 4x flash = 32768 words, 4x RAM = 4096 bytes, ~£1.95 DigiKey UK / ~£2 Mouser,
  both in stock) builds fine with a one-line `-mcpu` change and gives a very comfortable 24.8%
  flash. What was actually OBSERVED on the simulator: the firmware never sees PTT asserted (0 keyed
  runs, no STATE_BYPASS_SNOOP), so tools/simulate/test_first_dit.py aborts at clause (a); band-change
  timing was unchanged (1161/1705 ms against 1162/1699 ms). Reverted; the tree was byte-identical to
  the last commit.
  **Why the verdict is not sound:** it was reached from a single configuration, with no positive
  control - the exact method that then produced TWO wrong verdicts for the PIC18F47Q10 in the same
  session (an assumed `T2CLKCON` value, then a missing `IPEN`/priority enable), both of which blamed
  the simulator for firmware configuration that had never been written. The symptoms above are real,
  but "MPLAB does not model it equivalently" is not established by them: a wrong register value, a
  pin-table difference in the model, or a different interrupt-enable form would all look identical
  from outside. The unchanged band-change timing also implies the timer tick *was* running, which
  sits badly with a blanket non-equivalence claim.
  **Superseded 2026-09-23: PIC18F47Q10 is the shipping target and the only device, and flash pressure
  is addressed by the software levers under Remaining work.** This bullet is kept only as the record
  of how a device-swap verdict must NOT be reached.
- [HISTORY - superseded 2026-09-23; the port is done and the Q10 is the only device] PIC18F47Q10
  PROVED VIABLE AFTER TWO WRONG VERDICTS IN ONE DAY (2026-09-22). Both retractions are
  mine; the sequence is the lesson. (1) The bring-up wrote `T2CLKCON = 0x00` and *assumed* it meant
  Fosc/4 - `firmware/src/main.c` already used `T2CLKCON = 0x01; /* Fosc/4 */`. With `0x01` the timer
  counts (`T2TMR` `106` then `92`). (2) Interrupts appeared dead - two independent sources raised
  and cleared their flags under polling with `GIE=1`, but `g_isr_any` stayed `0`. That was also
  wrong: those builds left `IPEN = 0`. With `IPEN = 1` plus the source's priority bit
  (`IPR4bits.TMR2IP = 1`) the ISR runs at once - `g_isr_any` and `g_tick` both `56 -> 172`, and the
  polled counter drops to `6 -> 18` because the ISR consumes the flags first. Measured on the Q10:
  PTT reaches the firmware, outputs toggle, Timer2 counts, interrupt-on-change works, interrupts are
  dispatched. The two real differences from the PIC16F are that the Q10 needs the *priority*
  interrupt form (`IPEN = 1` + `IPRx`, not a plain `GIE = 1`) and that the tick calibrates at
  ~6,100-6,900 instructions against the harness's 8,000 for 1 ms. `OSCCON3.ORDY` reads `0` while the
  timer demonstrably runs - never cite it about the clock. **Superseded 2026-09-23: the port was
  done, PIC18F47Q10 is the only device in the project, and the 16F port has been removed.** Evidence:
  `bugfixes.md` 2026-09-22; branch `spike/pic18f47q10-retest` holds the images.
- The firmware is the single source of truth for which PIC we are using: PIC18F47Q10-I/P (40-pin PDIP), declared once in `cmake/My_Pic_Project/default/device.cmake` and emitted into `.generated/rule.cmake` as `-mcpu=18F47Q10` (mirrored in `.clangd`). If a document disagrees with the firmware about the target device, the firmware wins and the document is the bug.
- Do NOT reintroduce schematic automation. Both attempts were abandoned outright and every trace of them has been removed. Gone for good: the EasyEDA generator/SVG-validation pipeline (`easyeda_pro_generator.py`, `svg_renderer.py`, `visual_validator.py`, `watch_and_render.py`, `pic_amp_control_full.yaml`, `out/picampcontrol_easyeda_*.json`, and the `easyeda-agent` / `easyeda-copilot` routes), the KiCad MCP `schematic-design` skill with its `generated/` `.kicad_sch` tree and `mcp-server-kicad`, all node/npm scaffolding (`package.json`, `node_modules`, `@jlceda`), the workflow documents that described them, and every KiCad symbol/toolchain reference in the docs. The user's instruction: EasyEDA "was a disaster and can be removed entirely", then "same for kicad stuff. Another disaster... it can all go". Do not add generators, MCP schematic servers, `.kicad_sch`/`.eext` tooling or node/npm back, and do not re-add tool-specific symbol references to the pin map. The schematic package at docs/hardware/project_schematic_package/ is deliberately static design inputs (block diagram, connection table, component list, wiring checklist), captured by hand end to end. Netlist-verification tooling (exporting the EDA netlist and diffing it against `connection_table.csv`) was offered as a lighter-weight alternative and has been declined as well, so this means no schematic automation of any kind - not drawing generation, not verification.
- `PTT COMPLETE` is a hardware/firmware LCD state, not merely a graph label: show it only after RELAYS, TX_VCC, and TX_BIAS have all reached their configured active levels (active-low by default); keep it transient, then restore the user's saved home page.
- Keep verified changes committed and pushed promptly; generated simulator CSVs belong in `_build/My_Pic_Project/sim/csv/` and graphs in `_build/My_Pic_Project/sim/graphs/`. Commit+push as soon as a step is verified green - the only valid reason to hold a commit is a verdict that is still running, and then you read that verdict from its log file and commit the moment it is green. Always pass explicit paths to `git add` (never `git add .`).
- WHERE A FACT LIVES (so this file does not drift, and so nothing is written twice):
  *the general standing rules* - the ones that apply to every task here (never write "Hmm", terse
  output, never judge a run from the terminal, every run through the watchdog, commit-and-push often,
  delete logs once read, record every durable finding, PIC18F47Q10 only) - live in
  **`.github/copilot-instructions.md`**, which VS Code injects into EVERY request automatically. That
  file is deliberately short: each of its lines costs on every query, so it carries rules and pointers
  only, never detail. **Nothing loads THIS file automatically** - there is no hook or instruction that
  opens `Ai-Notes.txt`, so a session only reads it because these notes say to (or because the user
  says so). That is exactly why the rules that must never be missed are no longer only here.
- THREE NAMES, SHORT (2026-09-24, user request: *"just name each skill (with a short name) so I know
  what they do"*): skill **`build-test`**, skill **`editor-clangd`**, agent **`build-runner`**. The
  folder (skills) or filename (agents) IS the name - a skill file must be exactly `SKILL.md`, so its
  name can only live in the folder. A skill `name:` that does not match its folder fails SILENTLY, with
  no error anywhere; an agent with no `name:` takes its filename.
- THIS FILE'S NAME IS DELIBERATELY NOT A CONVENTION (user question 2026-09-24). VS Code's primitives
  are exactly: `.github/copilot-instructions.md` (== `AGENTS.md`; use ONE, never both),
  `.github/instructions/*.instructions.md`, `.github/prompts/*.prompt.md`, `.github/agents/*.agent.md`,
  `.github/skills/<name>/SKILL.md`. `Ai-Notes.txt` matches none of them, so **nothing loads it
  automatically** - it is read only because `.github/copilot-instructions.md` names it first and the
  user asks. That is the intent: this file is 499 lines / ~57 KB of session state, and renaming it to
  an always-on primitive would inject all of it into EVERY request (the "kitchen sink" anti-pattern).
  State stays here non-auto-loaded; the rules that must never be missed stay in the 48-line
  `copilot-instructions.md`. Do not "fix" the extension.
- `mistakes.md` DELETED 2026-09-24 (user question: *"shouldn't the mistakes.md content be in the build
  skill and mistakes.md deleted?"*). It was never a mistake log - it was ONE pasted paragraph (the file
  had a single line, no newlines) from another agent correcting a false claim that interrupts are
  unimplemented in MDB. The durable part - the core and the interrupt controller ARE modelled,
  `W0106-SIM` is scoped strictly to PPS/clock-source routing, and the workaround is to poke the
  peripheral/flag registers directly behind `#ifdef __MPLAB_DEBUGGER_SIMULATOR` - now lives in
  `.github/skills/build-test/SKILL.md` with the rest of the simulator method. `blockers.txt` and
  `Eeprom-changes.md` were deleted in the same pass: both were superseded AI pastes, and blockers.txt's
  two diagnoses were BOTH wrong (step-count maths superseded by the measured rate; the RA6/FEXTOSC
  theory disproved when ANSELA6 turned out to be already clear). Supersessions are in `bugfixes.md`.
- DOC HYGIENE (2026-09-24, from the review of every .md and skill). Canonical build directories:
  `_build/My_Pic_Project/release` is what the editor reads, `_build/My_Pic_Project/sim` is where the
  tests run. The `q10_*` trees and the two Q10 VS Code tasks are GONE - the Q10 is the default device,
  so it needs no build tree of its own. `TESTING.md` had been teaching a bare `test_first_dit.py`,
  which is the cheat, and now shows the wrapped form. The README's clock line said 32 MHz/HFINT32: the
  core is 64 MHz (`HFINTOSC_64MHZ`) and Timer2 takes Fosc/8 so `_XTAL_FREQ` stays the 32 MHz design
  clock. **Rule: never restate a rule in two files unless one is a pointer** - the copies drift, and
  whichever a session reads first wins, which is how `TESTING.md` came to outrank `user.cmake`.
  **The temporary "do not change the MCU" gates are GONE from this file (same day).** There is no rule
  about asking before a device change and no rule about a dedicated branch for one: those were written
  for the evaluation and were never meant to be permanent. The current target statement stands on its
  own - PIC18F47Q10 is what the firmware builds, and `device.cmake` is where that is declared.
  *state and user instructions* (what the firmware does now, what is in flight, what is left, the rules below) live
  in THIS file; *build/test/debug procedure* (toolchain paths, configure/build commands, the flash
  budget, mdb and harness techniques, test names and labels, failure triage) lives only in
  `.github/skills/build-test/SKILL.md`, with `.github/agents/build-runner.agent.md`
  as the thin runner that must stay in sync with it and TESTING.md as the human-facing guide;
  *editor/toolchain problems* (VS Code's Problems panel, clangd, the C/C++ extension,
  `compile_commands.json`, `-mdfp=`/`-mcpu=` complaints, XC8 device headers or missing family macros)
  live only in `.github/skills/editor-clangd/SKILL.md`, with the forensic detail in
  its `references/xc8-headers.md`. That is a SEPARATE skill on purpose: neither its rules nor its
  traps can affect a build or a test - XC8 is invoked directly and needs none of it - and keeping it
  out of the build/test skill makes that skill cheaper to read on every build and test task. The
  build/test skill keeps only a one-line pointer to it, and vice versa;
  *design detail* lives in `docs/` (e.g. docs/first-dit-band-detection.md); *defects and their fixes*
  in `bugfixes.md`; *historical/origin narrative* only in `prototype_reference/`. Never restate a
  command here - link to the skill.

## Date
2026-09-23

## Session handoff (carry-over for a NEW session)
- This is the ONLY AI carry-over file. `AI-HANDOFF.md` was deleted on 2026-09-21 (it duplicated
  project-architecture.md, the pin map and TESTING.md, and had drifted), and
  `docs/next-session-prompt.md` was retired to a tombstone on 2026-09-23 (it still told a new
  session to work on the now-deleted `upgrade/pic18f47q10` branch and to chase a Q10 temperature
  trip that had long since been fixed). Do not recreate a second handoff file - update this one.
- Working tree should be clean; read the current commit with `git --no-pager log --oneline -1`
  rather than trusting a SHA written here.
- **The project now runs on Windows 10 as well as macOS** (MPLAB X 6.35 / XC8 4.00 / Python 3.14.7).
  MDB is ~2.4x slower on Windows: the merged suite is ~356 s there against ~150 s on macOS. All
  platform-specific harness code lives in `tools/simulate/platform_process.py`. Build/test procedure,
  the Windows gotchas and the timeout policy are in the skill - do not restate them here.
- Last verified run (Windows): `ctest --test-dir _build/My_Pic_Project/debug --output-on-failure`,
  verdict read from the log file: `100% tests passed, 0 tests failed out of 2`, `CTEST_EXIT=0`, total
  408 s (PTT_SequencerAndTripSuite 355 s, FirstDit_BandDetectionAndHotSwitchGuards 53 s).
- Last verified run (macOS, 2026-09-23, on the Q10-only tree) - THE FIRST macOS RUN OF EITHER TEST:
  * Configure + Release build GREEN: `PicAmpControl device: PIC18F47Q10 (mcpu=18F47Q10)`, DFP from
    the MPLAB X install, `BUILD_EXIT=0`, ELF at `out/My_Pic_Project_18F47Q10/default.elf`.
  * `PTT_SequencerAndTripSuite`: **PASS**, twice - `PTT suite passed: 11 scenarios in one MDB
    session` (log `/tmp/pac_mac_suite2.log`), ~110 s. All I1-I6 invariant lines PASS in every
    scenario.
  * `FirstDit_BandDetectionAndHotSwitchGuards`: **FAIL on macOS.** Invariants I1-I6 PASS, clauses
    (a) and (b) PASS, then `AssertionError: clause (c): the warm-start PTT never keyed the
    amplifier` (tools/simulate/test_first_dit.py line 375). Clause (c) re-keys on the remembered
    band with no RF stimulus at all and requires the amplifier to key within 30 ms of PTT being
    asserted. This is the open item - Windows passed the same clause, so it is either a real
    macOS-only difference or a harness timing assumption that macOS exposes. Evidence in
    `bugfixes.md` 2026-09-23 (including the two things the transcript shows: the harness cannot read
    the settle/verify globals in this run - they print as `?` - and a 20m keyed run DOES appear
    later, at 1917-2086 ms, so keying itself works).
  * Reading verdicts: the agent's terminal harness SIGTERMs the wrapper once the terminal is
    cleaned up, so the wrapper's last line reads `SUITE_EXIT=143` even for a completed run. The
    harness's own printed lines (PASS/FAIL, the assertion, the scenario list) are the verdict.
- The standing process rule that produced the most friction last session: never judge a test run
  from terminal output. MDB emits megabytes of trace and a command that "returned no output" can
  still have passed - always redirect to a log file, append the exit code, and read the file.
- Work landed in the 2026-09-22 session (all committed and pushed; details in `bugfixes.md`):
  * `tools/simulate/platform_process.py` (new), with all four harnesses rewired through it - the
    simulator workflow did not run on Windows at all before this. Five defects fixed.
  * Timeouts re-sized for the slower platform: `run_mdb(timeout=1500)` as a last-resort net sitting
    above the launcher's `--timeout 1200`. Do not shrink them back to macOS-sized numbers.
  * `docs/hardware/PIC16F18875_pin_map.md`: every physical pin number was wrong and is now corrected
    against the datasheet; the "RD2-RD7 are free" error and the phantom VREF+ pin are gone.
  * The supply rail is stated explicitly as 5 V (the map had mislabelled it 3.3 V).
  * SCL/stimulus claims corrected: MDB SCL is NOT usable here, so the frequency counter remains
    verified only by injecting TMR1H/TMR1L.
  * The PIC18F47Q10 spike was RUN on `spike/pic18f47q10` with the user's go-ahead. It produced TWO
    wrong verdicts, both retracted the same day: first "no time base" (an assumed `T2CLKCON = 0x00`
    where `firmware/src/main.c` uses `0x01`), then "no interrupt dispatch" (the builds left
    `IPEN = 0`; with `IPEN = 1` plus `IPR4bits.TMR2IP = 1` the ISR runs immediately). The device does
    the basics: PTT, outputs, Timer2, interrupt-on-change and interrupt dispatch all work. Remaining
    real differences for a port: the priority interrupt form, and a tick that calibrates at
    ~6,100-6,900 instructions rather than the harness's 8,000. Branch `spike/pic18f47q10-retest`
    holds the images; `bugfixes.md` 2026-09-22 holds the evidence.
  * The skill gained the Windows shell traps that silently no-op a build (nested `cmd /c` quoting, a
    mangled multi-line call that returned exit 0 and compiled nothing) and the rule to look for DFPs
    inside the MPLAB X install before concluding a device is unbuildable.

## Simulator timing limitation (IMPORTANT)
- The firmware uses Timer2 for the approximately 1 ms scheduler tick because MDB's Timer0 model stalled during earlier testing.
- The simulator is still a debugger model, not a substitute for silicon timing. The suite validates state ordering, LCD lifecycle, trip latching, and recovery behavior; bench validation remains required for exact hardware timing.
- **Timer1 external clock (T1CKI) is NOT modelled** - the simulator reports "W0106-SIM: This device only has partial support for TMR1 peripheral ... timer clock selection is not implemented". Verified 2026-09-21: with RD1 actively driven by an SCL stimulus (`stim <file.scl>`; `print pin RD1` = HIGH/Din) and T1CON correctly set to T1CKI with the 1:4 prescaler (0x27), TMR1L/TMR1H stayed at 0. So a true end-to-end RF frequency-counter test is IMPOSSIBLE in sim, and no stimulus mechanism can fix it - the peripheral model is missing. The suite therefore injects TMR1H/TMR1L directly, which covers the frequency maths, band classification, band-select outputs and TX lock, but not the T1CKI pin, PPS routing, prescaler, or Timer1 overflow path. Do not "fix" this by trying to clock the pin.
- MDB SCL / `stim`: NOT USABLE - RE-TESTED 2026-09-22, and the earlier note here that claimed SCL was available is WRONG. `help stim` does document `stim <file>` ("Loads the specified SCL stimulus file into the simulator"), but this MDB build rejected two plausible SCL forms: the `configuration for "pic16f18875" is / end configuration;` block plus a named `testbench`/`process` gave `Error: syntax error`, `Error: stack underflow. aborting...` and `E0101-SIM: Failed to disassemble instruction (line 3)`; the same file with the configuration block removed failed at line 1. The working syntax was never recorded by whoever claimed it worked, and it could not be reconstructed from the help. Treat SCL as unavailable - do not cite it as a route to testing anything - until someone shows a file that actually loads. That also means the earlier `<=` vs `:=` syntax detail is unverified, and a stimulus that claims to drive a pin should be treated as undelivered until `print pin <name>` proves otherwise. Independently settled regardless: no stimulus of any kind can clock Timer1 from T1CKI, because the timer clock-source mux is not modelled (see the T1CKI note above). An SCL script that drives RC0 cannot test the frequency counter in any case - RC0 is INPUT_PTT in this design; T1CKI is RD1 via PPS.

## Current design
This repository is the active PIC16F18875-I/P linear-amplifier protection controller. Obsolete prototype source files have been removed so they cannot enter the production build.

## Firmware
- **First-dit band switching is implemented** (docs/first-dit-band-detection.md). With no decoded
  band, PTT is latched but the amplifier stays in bypass (STATE_BYPASS_SNOOP, g_snoop_active) while
  the radio's first RF burst is decoded; the band is then remembered (g_band_cache_valid/
  g_band_cache_band) so later PTT drops engage instantly, and the memory is dropped after
  BAND_CACHE_IDLE_TIMEOUT_MS (60 s, needs bench confirmation) of inactivity. BAND_SETTLE_MS (20 ms,
  needs bench confirmation) holds bypass after the decode so the amplifier is never keyed into a
  relay that is still moving. apply_bypass()/release_band_if_cold() enforce the "relays only move
  with the amplifier cold" rule, and g_band_established gates keying: the amplifier never keys
  unless a band is backed by a real measurement or the first-dit memory for this transmission
  (that is what stops it keying on the classifier's 160m no-signal default after a trip
  recovery). New freq_counter API: freq_counter_restore_locked_band(),
  freq_counter_band_confirmed(), freq_counter_measured_band().
- **TWO mechanical relay groups in the RF path, and the order they switch in.** `OUTPUT_TX` (RC5,
  labelled `RELAYS` by the harness) is the **T/R relay** that puts the amplifier into the path;
  `K1-K6` (RD2-RD7) are the **LPF band relays**. The band relays must be settled *before* the T/R
  relay closes, so the T/R relay can no longer be the first thing fired on PTT-low when the band is
  not yet known (that is why first-dit bypass exists, and why the normal "relays first, wait, HT,
  bias" order only holds once the band is known). The decode path holds BAND_SETTLE_MS after
  commanding a new selection; the warm path now does the same whenever
  `freq_counter_restore_locked_band()` reports the selection actually moved. The LPF filters are on
  the **TX train only** - RX does not pass through them - so the rig never sees the band relays
  change over while the T/R relay is open.
- **The relay selection no longer follows silence.** `freq_counter_tick_10ms()` drives the band
  outputs only for a usable measurement; it used to follow the classifier's 160m no-signal default,
  which parked the relays on 160m after every over and made almost every warm re-key move them.
- **The SWR trips are armed only while the TX path is engaged** (`g_sequence_stage` 1-3, the stages
  that hold `OUTPUT_TX` asserted). They used to be evaluated unconditionally, so a bridge reading
  during bypass (relay open, band selection possibly moving) could latch a spurious STATE_TRIP
  during the very first dit. Hardware overcurrent, current, temperature, overdrive and drain trips
  remain ungated. The gate is applied once inside `update_protection_state()` for flash reasons.
- **FLASH IS EFFECTIVELY FULL IN THE Debug/simulator BUILD.** The shared Debug image links at
  8189/8192 words (100.0%, 3 words free) - up from 8135/8192 before the relay-ordering and
  SWR-arming work. Release (the shipping budget) still has normal headroom, but the simulator build
  has none, and the linker fails with `(1347) can't find 0xN words ... for psect` as soon as
  anything is added. The failing psects are string literals (`STRCODE`), so the cheapest room is in
  the LCD text. **Space was therefore freed on 2026-09-21** by shortening every `g_setting_menu_labels`
  entry (e.g. `S1 SWR TRIP` -> `SWR1 TRIP`, `NTC B VALUE` -> `NTC B`, `CURRENT TRIP` -> `CURR TRIP`,
  `TX-VCC DELAY` -> `TX-VCC DLY`, `TX-VCC ACTIVE` -> `TX-VCC POL`, `POWER DISPLAY` -> `POWER MODE`,
  `PEAK HOLD` -> `PK HOLD`) and reducing the boot message to a single `Booting` line - about 60
  words reclaimed. `tools/simulate/render_lcd_lifecycle_diagram.py` was updated to match, since its
  SETTINGS_PANELS hard-code the on-screen labels. The budget rules and how to read the live figure
  are in the skill's `## Flash space`; the current numbers are in the Device memory usage section
  below.
- **The remembered band is verified, not trusted (fold-back).** A first-dit engage taken from the
  band memory is a blind decision - at keydown the radio has not started transmitting, so there is
  nothing to measure yet. The remembered band is therefore re-checked against the first usable
  measurement of that same transmission: if the measured band differs for BAND_VERIFY_MS (20 ms),
  the firmware forces bypass FIRST, releases the band, and lets the snoop path re-select the band
  actually being received cold before re-engaging. This is what makes the user's "FirstDit on 160m,
  then immediately change to 80m and key straight away" case safe: the amplifier never amplifies the
  whole first over through the wrong filter. Measured fold-back latency in the simulator: 23 ms.
  The relay selection is never moved while keyed (the verify step bypasses before it unlocks).
- Direct ADC inputs monitor two forward/reflected SWR pairs, temperature, WCS1700 current, input power, and drain voltage. Which channel is which lives ONLY in docs/hardware/PIC16F18875_pin_map.md - never restate that list here.
- ADC configuration (firmware/src/main.c adc_init): 10-bit, right-justified legacy format, VDD-referenced with the internal FVR off. Raw values are consumed directly as plain 0-1023 counts, so at a 5 V rail one count is about 4.88 mV.
- Current-sensor scaling (firmware/src/main.c): zero at raw 512 (2.5 V mid-rail), +511 counts = +70 A, 0 counts = -70 A. The default positive current trip is 40 A (g_thresholds initialiser).
- On PTT release the sequencer unwinds in order - TX off immediately, TX_VCC after the VCC delay, then TX_BIAS - and the band is released only once every TX output is confirmed inactive (release_band_if_cold()). An engage still in progress when PTT releases is therefore unwound, not completed; the old "engages complete, then unsequence" description is obsolete (see bugfixes.md 2026-09-21).
- SWR trips, power scales, drain/input thresholds, 10 kOhm NTC B-value profile, sequencer timing, and output polarities are user-configurable from the 1602 LCD menu.
- Outputs are written through the `LAT` registers and inputs are read from `PORT` (`deepseek-pic.md`
  guardrail 1). `pin_map.h` holds the write macros; the three TX outputs also carry `SENSE_TX*`
  pin-level read macros, used by the two checks that must confirm the hardware actually reached a
  state (`release_band_if_cold()`, and the PTT COMPLETE condition) instead of trusting the latch.
  Do not reintroduce `PORTxbits` writes for outputs: on PORTC the relay lines share a port with the
  LCD data lines, so a port read-modify-write can clobber a relay latch from an unrelated write.
- Hardware comparators combine at INPUT_HARD_FAULT for independent overdrive, drain-peak, and overcurrent protection.
- The sequencer runs TX, TX_VCC, and TX_BIAS with two configurable inter-stage delays, defaulting to 20 ms.
- The LCD is a 1602 driven in 4-bit parallel mode by firmware/src/lcd_parallel.c, with its interface header at firmware/include/lcd_parallel.h (renamed from lcd_i2c.h on 2026-09-22 - a prototype-era name that kept misleading readers; there is no I2C backpack and no I2C code). Internal EEPROM access lives in firmware/src/nvm.c + firmware/include/nvm.h, device-guarded so the PIC16F18875 keeps XC8's legacy eeprom helpers while the PIC18F-Q10 drives its own NVM block. Pin assignments: see the single-source-of-truth rule above.
- Menu settings and the last display page are stored in the PIC's internal EEPROM in a versioned, checksummed record; no external EEPROM module is used.

## Display
- The default home page is the PEP/temperature display.
- The STATUS page shows post-filter forward power as PEP or RMS, two-decimal SWR, and a full-width `|`/`.` bar.
- The PEP/temperature and current-meter pages use EEPROM-saved peak hold/decay settings: factory defaults are 1.2 s hold and 100 ms smooth decay interval.

## Simulation
- Both build configurations link to the SAME `out/My_Pic_Project/default.elf`. After a Release build the debug ELF (and the symbols the harness reads with `print <var>`) is gone, and Ninja says "no work to do" for Debug, so delete the ELF and rebuild Debug before running the simulator tests: `rm -f out/My_Pic_Project/default.elf && cmake --build _build/My_Pic_Project/debug -j4`.
- The firmware can be simulated instruction-accurately on the exact PIC16F18875 using MPLAB X's headless `mdb` debugger (gpsim does NOT support this device - its newest PIC16F1 parts are the 1788/1823/1825/1847 family, missing this chip's ADCC/CLC peripherals).
- Cross-platform tooling: macOS uses `tools/simulate/*.sh`, Windows uses `tools/simulate/*.ps1` (same behaviour). `cmake/My_Pic_Project/default/.generated/toolchain.cmake` and `.generated/rule.cmake` auto-detect XC8/DFP pack locations per-OS (macOS: `$HOME/tools/microchip/...`; Windows: `C:/Program Files/Microchip/xc8/...` and `%USERPROFILE%/.mchp_packs`, since `$ENV{HOME}` isn't set on Windows) - no manual cache overrides needed on either OS.
- The BASH scripts are not the whole story: the Python harnesses used to be POSIX-only. Since 2026-09-22 all platform differences live in `tools/simulate/platform_process.py` (`temp_dir`, `find_mdb`, `isolated_spawn_kwargs`, `pid_is_alive`, `terminate_tree`/`kill_tree`, `running_processes`), which every harness imports. Do not put `start_new_session`/`os.killpg`/`signal.SIGKILL`/`ps`/`/tmp` back into a harness - the first three raise `AttributeError` or are silently ignored on Windows, and `os.kill(pid, 0)` actually KILLS the process there. See bugfixes.md 2026-09-22.
- Verified 2026-09-22: the full test workflow (configure, Debug build, both CTest tests including the first-dit proof) passes on Windows 10 with Python 3.14.7 / MPLAB X 6.35 / XC8 4.00, and the Debug image is bit-for-bit the same size as on macOS (8117/8192 words).
- IMPORTANT build-flag fix (2026-09-15): `cmake/My_Pic_Project/default/user.cmake` used to unconditionally append `-Os`, which silently overrode the Debug preset's `-O0` (last `-O` flag wins) - so "debug" builds were actually optimized, breaking mdb breakpoints/symbols (tiny functions got inlined, statics got stripped). Now `-Os` only applies via `$<$<CONFIG:Release>:-Os>`. If mdb breakpoints on functions/statics mysteriously don't resolve again, check this file first.
- macOS setup: `brew install --cask mplabx-ide mplab-xc8`.
- Windows setup: install the full MPLAB X IDE (not just MPLAB IPE) so `mdb.bat` (under `mplab_platform/bin`) is present. The already-installed VS Code extensions `microchip.mplab-core-da` (Debug Adapter for MPLAB) and `microchip.mplab-data-visualizer` give a GUI debug/scope experience but both ultimately drive the same `mdb`/simulator engine, so scripted simulation doesn't require them.
- `tools/simulate/build_firmware.sh` (macOS) / `build_firmware.ps1` (Windows) builds firmware locally into `_build/My_Pic_Project/sim` / `out/My_Pic_Project/default.hex`.
- `tools/simulate/run_sim.sh` / `run_sim.ps1 [scenario.mdb]` builds, then launches `mdb` against device `PIC16F18875` with the hex programmed, filtering the benign `W0106-SIM` TMR1/3/5 warnings (see below) out of the output. With no argument it's an interactive `mdb` session; with a scenario file it runs those commands then exits. `write pin <name> high|low|<N>v` to drive inputs, `print pin <name>` to read outputs, `Stepi <count>` to single-step, `break <function>`/`break <file>:<line>` + `Run`/`Continue`/`Halt` for real breakpoint-based debugging, `Stopwatch [nror]` for real simulated elapsed time. Example scenarios: `tools/simulate/scenarios/boot_smoke.mdb`, `ptt_cycle.mdb`.
- mdb command files do not support `;`/`#` comment lines - an invalid line aborts the rest of the script, so keep scenario files to plain commands only.
- A VS Code GUI debug session (breakpoints/variables/watch/registers, no terminal) is available via `.vscode/launch.json`'s "Simulate PicAmpControl (Debug)" config, using `microchip.mplab-core-da` against `Simulator`/`PIC16F18875`. Requires `out/My_Pic_Project/default.elf` to exist (build first). Pin/register live-watch: add `PORTA`/`PORTB`/`PORTC`, `LATA`/`LATB`/`LATC`, `TRISA`/`TRISB`/`TRISC` (or bitfields like `PORTCbits.RC5`) to the Watch panel manually - there is no file-based way to pre-seed Watch expressions.
- `main.c` explicitly clears `ANSELC` so RC0/RC3-RC7 operate as digital I/O; keep this initialization when changing the port map.
- Benign, expected simulator noise: `W0106-SIM: This device only has partial support for TMR1/TMR3/TMR5 peripheral...` on every reset. Firmware never touches T1CON/T3CON/T5CON; this is just `mdb`'s partial clock-source-mux model for those timers at their POR default state, not a real firmware/silicon issue - safe to ignore (already filtered out by run_sim.sh/.ps1).
- FIXED (2026-09-15, see bugfixes.md): the ADC ISR used to immediately re-arm the next conversion (with a blocking `__delay_us`) right after each conversion completed, entirely inside `timer0_isr()`. In simulation this created a back-to-back interrupt chain that fully starved the main loop - `update_tx_sequence()`/`update_protection_state()` never ran even once after 30+ simulated seconds. Fixed by only re-arming once per Timer0 tick (~1 ms) instead of immediately; verified main-loop breakpoints now hit normally. If future simulation runs show the CPU stuck at the interrupt vector address on every `Halt` with continuous `W0223-ADC` spam, that class of bug is back.
- To simulate a steady-state-then-PTT-drop scenario without spurious protection faults, drive plausible idle ADC voltages before running: `write pin RA0/RA1/RA2/RA3 0v` (SWR fwd/ref, both pairs), `write pin RA5 2.5v` (temp), `write pin RB1/RB2/RB3 0v` (current/overdrive/drain), `write pin RB4 low` (hw overcurrent fault, active-high). PTT (`RC0`) is active-low; hold `high` for idle/steady-state, then `low` to assert. Without explicit stimulus, floating ADC inputs read as very low/high and spam `W0222/W0223-ADC` over/underflow warnings (cosmetic) but can also cause `swr_trip()` etc. to behave unpredictably.
- The simulator suite runs eleven scenarios in one MDB session and emits per-scenario CSVs and graphs under `_build/My_Pic_Project/sim/csv/` and `_build/My_Pic_Project/sim/graphs/`. The first-dit proof is a separate session (`tools/simulate/test_first_dit.py`, `--csv` to dump its samples) and shares the band-selection invariant checks in `tools/simulate/first_dit_invariants.py` with the suite.
- Injecting snoop RF only *before* PTT models a radio that is silent while transmitting. The tick resets TMR1 every 10 ms, so the injected measurement goes stale and the firmware correctly stays in bypass. Keep the band frequency injected for the whole keyed window (the suite does this for 40m now; see bugfixes.md 2026-09-21).
- mdb cannot write a C variable by name in this version (`write g_x 1` fails with `For input string: "<addr> "`, and `print /a g_x` fails the same way). Fault injection into firmware state works via the address: read it from `out/My_Pic_Project/default.sym` (line format `_g_name <hexaddr> 0 BANKn <size>`, e.g. `_g_band_cache_idle_ms B4 0 BANK1 1`) and use `write /r 0x<addr> <lo> <hi>` for a 16-bit value (little-endian, one byte per word) or `write /r 0x<addr> <byte>` for a bool/enum. Addresses move on every rebuild, so parse the .sym at run time (test_first_dit.py does).
- Run tests with `./run_tests.sh` (macOS) / `.\run_tests.ps1` (Windows), or `ctest --test-dir _build/My_Pic_Project/debug --output-on-failure > /tmp/pac_ctest.log 2>&1` (`*>` and `$env:TEMP` on Windows; see TESTING.md). A green run ends with `100% tests passed`; the suite prints `PTT suite passed: 11 scenarios in one MDB session` and the first-dit proof prints `FIRST-DIT PROOF PASSED: ...`. Roughly 3 minutes on macOS, ~7 minutes on Windows (MDB is ~2.4x slower there).
- CTest registers two tests (cmake/My_Pic_Project/default/user.cmake): `PTT_SequencerAndTripSuite` (labels `sim;suite`, runs `run_suite_with_watchdog.py --timeout 1200`) and `FirstDit_BandDetectionAndHotSwitchGuards` (labels `sim;first-dit`, runs `tools/simulate/test_first_dit.py`). Select with `-R FirstDit`, `-R PTT_Sequencer`, or `-L sim`; never run the two concurrently (both own MDB, and the suite launcher kills stray MDB processes). The suite is Python 3 only and configuration now fails if the discovered interpreter is not Python 3; `PYTHON_EXECUTABLE` is cached, so clear it with `-U PYTHON_EXECUTABLE` when reconfiguring an existing build directory.
- TIMEOUTS ARE SIZED FOR THE SLOWEST PLATFORM (2026-09-22): MDB is ~2.4x slower on Windows than macOS (suite 356 s vs ~150 s; first-dit ~55 s vs ~20 s). The old 280 s inner MDB timeout killed a perfectly healthy suite run on Windows while it was still printing progress. The budget is now `run_mdb(timeout=1500)` as a last-resort net, `run_suite_with_watchdog.py --timeout 1200` as the real budget, and matching CTest registration. Do not shrink them to macOS-sized numbers, and never read a timeout as a firmware regression - check the heartbeat log (`delta=0` and frozen `mdb_bytes` = hung; output still increasing = slow but healthy).

## References
- Hardware source of truth: docs/hardware/PIC16F18875_pin_map.md
- Architecture and safety behaviour: docs/project-architecture.md
- Band-change detection (first-dit model, implemented): docs/first-dit-band-detection.md
- Firmware entry point: firmware/src/main.c
- Bug fixes log (update whenever a real bug is found): bugfixes.md

## CI / release
- .github/workflows/firmware-build.yml builds on every push/PR to main on `ubuntu-latest` (installs XC8 v4.00 + DFP, Ninja) and packages `.hex`/`.elf`/`.map`/`.xml` plus the pin map (docs/hardware/PIC16F18875_pin_map.md) into a `firmware-<sha>` artifact. It compiles only - the simulator tests are not run in CI.
- .github/workflows/auto-release.yml runs after a successful main build, auto-increments a `v0.0.N` tag, and publishes that artifact as a GitHub Release. This is fully automatic - no manual step needed for a normal push to main.
- .github/workflows/release-firmware.yml is the manual fallback (workflow_dispatch) to re-publish an older build's artifact under an existing tag.

## Device memory usage
- `memoryfile.xml` (and `mem.map`) is regenerated by XC8 on every build and is always included in the latest release, so it always reflects the current firmware, not a stale snapshot.
- To check current % free: download it from the latest release (`gh release download <tag> -p memoryfile.xml`) and read `<memory name="program">`/`<memory name="data">` `used`/`free`/`length` (program = flash words, data = RAM bytes).
- FLASH IS TIGHT (2026-09-21): Release (shipping) uses ~6899/8192 words = ~84% used; the Debug build
  that the simulator/tests load is the binding one - it was 8135/8192 (99.3%) at the start of this
  session, peaked at 8189/8192 (100.0%, 3 words free) once the relay-ordering and SWR-arming guards
  landed, and is now 8117/8192 = 99.1% used (75 words free) after the LCD label/boot-text shortening.
  That is because user.cmake deliberately compiles Debug with -O1 (not -Os) so mdb can resolve
  symbols and breakpoints. Budget new firmware features against the Release number, and expect the
  Debug build to hit the 8192 limit first. Ask before adding code, and prefer table-driven logic
  over long if/else chains. Budget rules and the exact way to read the live figure: the skill's
  `## Flash space`.
- As of v0.0.25 (commit 3a49285): program flash 54.2% free (4440/8192 words), data RAM 84.2% free (862/1024 bytes). Superseded by the 2026-09-21 numbers above.

## Remaining work

### Open items carried out of the relay-ordering work (2026-09-22)

All three are OPEN. Each states what "done" means, so a later session can close it rather than
re-derive it.

1. **Prove clause (h) on its own.** Clause (h) checks that the LPF relay selection HOLDS through RX
   instead of following the 160m no-signal default. Re-introducing that defect does fail the test, but
   it fails *earlier*, at `clause (c): the remembered 20m band was not restored`, because one phase of
   the single MDB session feeds the next - so clause (h)'s own proof is outstanding. To close it, make
   the injection narrower: give clause (h) its own session, or restructure the phases so (a)-(c) do not
   depend on the selection surviving the previous release. Either way record the observed failure
   message in the defect table in docs/first-dit-band-detection.md.
2. **Measure per-function flash, then cut the biggest genuinely safe win.** Do NOT trust the earlier
   "table-drive `adjust_selected_setting()`, save 80-100 words" estimate: several of those settings are
   16-bit, so a closed-form `{min,max,step}` row costs ~6 bytes and a 20-page table is ~120 bytes plus
   lookup code - probably MORE than the branch chain it replaces. Measure real sizes first (the map's
   psects, or build-delta experiments on a scratch branch), then implement only what actually wins.
   Policy, lever list and the inline-assembly rule: the skill's `## Flash space`.
3. **Bench-confirm the relay timings and the Timer1 path.** The 20 ms relay assumption is what both
   `BAND_SETTLE_MS` and `tx_vcc_delay_ms` rest on, and invariant I6 asserts only a 10 ms floor
   (deliberately below 20 ms and above the 1-6 ms a defect produces) at 1-5 ms sampling, so the margin
   is thin: measure the fitted K1-K6 and T/R relay operate times and confirm or adjust both constants.
   Separately the simulator does NOT model Timer1's external clock (`W0106-SIM`), so T1CKI, the PPS
   routing, the 1:4 prescaler and the overflow path are untested - the first burst is injected by
   writing `TMR1H`/`TMR1L`. That gap closes on the bench only.

### General

The open work is hardware validation: final sensor calibration, comparator thresholds and polarity, ADC transient protection, TX timing, fan implementation, LCD/menu/internal EEPROM persistence testing, final PCB review, and CI validation. First-dit specifics to confirm on the bench: BAND_CACHE_IDLE_TIMEOUT_MS (60 s) against real band-change habits, BAND_SETTLE_MS (20 ms) against the fitted LPF relay's operate time, and BAND_VERIFY_MS (20 ms) - i.e. how long a wrong remembered band may stay engaged before fold-back, which is the one figure that trades amplifier protection against nuisance drop-outs on a noisy first dit.

Flash space is the next constraint. Measure before you refactor - the earlier estimates here were
guesses and at least one is probably wrong:

1. Make `adjust_selected_setting()` in firmware/src/main.c table-driven ({min,max,step} rows instead
   of the long if/else chain). **The earlier "est. 80-100 words" is suspect.** Most of those settings
   are 16-bit, so a `{min,max,step}` row costs ~6 bytes and a table for all 20 pages is ~120 bytes
   *plus* lookup code - likely MORE than the branch chain it replaces, whose branches are a handful
   of words each. Measure the actual function size first; if the table does not win, say so and drop
   the idea rather than implementing it for appearance.
2. ~~Compile the translation units the harness does NOT read (`lcd_parallel.c`) at `-Os` in the Debug
   build.~~ **MEASURED 2026-09-22: this reclaims NOTHING - do not retry it.** The per-source override
   does apply and the file genuinely recompiled, but the Debug image stayed at 8117/8192 words exactly.
   XC8's linker optimises at the `-O1` in the link rule, normalising the per-file level, so per-TU `-O`
   overrides are the wrong layer. Detail and the reproduction: the skill's `## Flash space`.
3. Compile the whole simulator/tests image `-Os` instead of `-O1` (user.cmake). Est. 1200 words off
   the Debug number, but degrades mdb symbol/breakpoint resolution for the VS Code "Simulate
   PicAmpControl (Debug)" session - the whole reason Debug is -O1 today. Needs explicit user consent.
4. Bit-pack the menu metadata, shorten the remaining display strings, or use commented inline
   assembly where a hot path justifies it (equivalent C must be in the comment).

Do not start a structural one without asking; see the skill's `## Flash space` for the policy and the
Device memory usage section above for the current numbers.
